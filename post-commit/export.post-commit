#!/bin/bash
# Exports the files in the repository to the GIT_WORK_TREE location.

unset GIT_INDEX_FILE
export GIT_WORK_TREE=/home/chris/
export GIT_DIR=/home/chris/sandbox/dotfiles/.git/
git checkout -f
